<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Biodata | Minuman Segerrr</title>

  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap');

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Inter', sans-serif;
    }

    body {
      min-height: 100vh;
      background: linear-gradient(135deg, #f2f5ff, #ffffff);
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .container {
      padding: 20px;
    }

    .card {
      background: rgba(255, 255, 255, 0.9);
      backdrop-filter: blur(14px);
      border-radius: 28px;
      padding: 36px;
      max-width: 400px;
      text-align: center;
      box-shadow: 0 30px 55px rgba(0,0,0,0.08);
      animation: slideUp 0.9s ease;
    }

    .card img {
      width: 150px;
      height: 150px;
      border-radius: 50%;
      object-fit: cover;
      margin-bottom: 20px;
      border: 4px solid #eef2ff;
    }

    h1 {
      font-size: 23px;
      font-weight: 600;
      margin-bottom: 8px;
    }

    .school {
      font-size: 14px;
      color: #555;
      margin-bottom: 18px;
    }

    .school a {
      color: #4b6bff;
      text-decoration: none;
      font-weight: 500;
    }

    .desc {
      font-size: 14px;
      line-height: 1.65;
      color: #333;
      margin-bottom: 26px;
    }

    button {
      background: linear-gradient(135deg, #4b6bff, #7f9cff);
      color: #fff;
      border: none;
      padding: 13px 34px;
      border-radius: 999px;
      font-size: 14px;
      cursor: pointer;
      transition: 0.3s ease;
    }

    button:hover {
      transform: translateY(-3px);
      box-shadow: 0 15px 28px rgba(75,107,255,0.35);
    }

    @keyframes slideUp {
      from {
        opacity: 0;
        transform: translateY(26px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>
<body>

  <main class="container">
    <section class="card">
      <img src="6574220a-7ead-4b10-8da7-0e2de8d3e86a.png" alt="Foto Biodata">

      <h1>Nisa Azki</h1>

      <p class="school">
        Student at Pesantren Modern
        <a href="https://azkiverse.my.id" target="_blank">SMA At taqwa</a>
      </p>

      <p class="desc">
        Website Minuman Segerrr adalah platform sederhana yang menampilkan
        konsep minuman segar, ide rasa, dan visual produk.
        Website ini dibuat sebagai media promosi kreatif
        dengan tampilan modern, minimalis, dan mudah diakses.
      </p>

      <button id="visit">Kunjungi Website</button>
    </section>
  </main>

  <script>
    document.getElementById("visit").addEventListener("click", () => {
      window.open("https://azkiverse.my.id/minumansegerrr/", "_blank");
    });
  </script>

</body>
</html>
